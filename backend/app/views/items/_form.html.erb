<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :barcode %><br />
    <%= f.text_field :barcode %>
  </div>
    <% @item.ingredients.each_with_index do |ingredient, index| %> <%=  index %>
        <% f.fields_for :ingredient_attributes, ingredient do |g| %>
            gdfkj
            <p><%= g.text_field :name %></p>
        <% end %>
    <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
